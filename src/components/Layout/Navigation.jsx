import React from 'react';

// Componente desativado para evitar duplicação de links de navegação
const Navigation = () => {
    // Retorna null para que nenhum conteúdo seja renderizado
    return null;
};

export default Navigation;